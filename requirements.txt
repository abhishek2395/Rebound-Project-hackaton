# Core runtime
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
httpx>=0.27
tenacity>=8.2
python-dotenv>=1.0
python-multipart>=0.0.9  # required by FastAPI's Form(...) parsing on POST /sms

# Not currently called anywhere in the runtime (ranking is fully deterministic
# by design — see RELIABILITY_BRIEF.md). Left here only in case that changes.
# anthropic>=0.34

# Test
pytest>=7.4

# Live Google Workspace integration (Calendar + Gmail).
# Only needed when running scripts/setup_google_oauth.py and manual_{calendar,gmail}.py
# — the 30-scenario harness itself runs against fakes without these.
google-api-python-client>=2.120
google-auth-oauthlib>=1.2

# Optional live Twilio integration.
# twilio>=9.0
# twilio>=9.0
