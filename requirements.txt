# Core runtime
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
httpx>=0.27
tenacity>=8.2
python-dotenv>=1.0

# Model provider (used at runtime; fakes work without a key)
anthropic>=0.34

# Test
pytest>=7.4

# Optional live integrations — only needed for `pytest -m live`
# Uncomment when running the real-sandbox smoke tests.
# google-api-python-client>=2.120
# google-auth-oauthlib>=1.2
# twilio>=9.0
